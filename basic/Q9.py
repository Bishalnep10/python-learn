"""
9. Iterating with Enumerate:

Use a for loop with the enumerate function to print both the index and value of each
item in the list colors = ["red", "green", "blue"].
"""
colors = ["red", "green", "blue"]

for index, color in enumerate(colors):
    print(f"Index: {index}, Value: {color}")
